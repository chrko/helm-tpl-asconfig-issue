This serves as an auxiliary repository to the reported issue in the Helm project.

Issue report: https://github.com/helm/helm/issues/13501

Template the examples with helm:

```bash
helm template --values values.yaml .
```
